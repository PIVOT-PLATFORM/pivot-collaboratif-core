# Applique .mvn/settings.xml (credentials GitHub Packages pour "github" et
# "pivot-core-packages", voir ce fichier) à toute invocation de mvn/mvnw dans ce repo —
# CI, build Docker, dev local — sans avoir à répéter --settings partout.
--settings
.mvn/settings.xml
