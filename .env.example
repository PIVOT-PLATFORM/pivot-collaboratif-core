# Copier en .env et adapter les valeurs
# Ne jamais commiter .env

# PostgreSQL — instance PARTAGEE avec les autres modules pivot-xxx-core (schema "collaboratif")
POSTGRES_PASSWORD=pivot_dev

# CORS
CORS_ALLOWED_ORIGINS=http://localhost

# Port du service (table des ports pivot-docs/docs/architecture/platform-overview.md)
SERVER_PORT=8083
